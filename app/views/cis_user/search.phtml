<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<?php use Phalcon\Tag; ?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(["cis_user/index", "Go Back"]); ?></li>
            <li class="next"><?php echo $this->tag->linkTo(["cis_user/new", "Create "]); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Search result</h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
            <th>Uname</th>
            <th>Passwd</th>
            <th>Openid</th>
            <th>From</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $cis_user): ?>
            <tr>
                <td><?php echo $cis_user->id ?></td>
            <td><?php echo $cis_user->uname ?></td>
            <td><?php echo $cis_user->passwd ?></td>
            <td><?php echo $cis_user->openid ?></td>
            <td><?php echo $cis_user->from ?></td>

                <td><?php echo $this->tag->linkTo(["cis_user/edit/" . $cis_user->id, "Edit"]); ?></td>
                <td><?php echo $this->tag->linkTo(["cis_user/delete/" . $cis_user->id, "Delete"]); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo("cis_user/search", "First") ?></li>
                <li><?php echo $this->tag->linkTo("cis_user/search?page=" . $page->before, "Previous") ?></li>
                <li><?php echo $this->tag->linkTo("cis_user/search?page=" . $page->next, "Next") ?></li>
                <li><?php echo $this->tag->linkTo("cis_user/search?page=" . $page->last, "Last") ?></li>
            </ul>
        </nav>
    </div>
</div>
